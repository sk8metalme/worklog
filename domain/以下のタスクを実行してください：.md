# 以下のタスクを実行してください：

**プロジェクト**: `/Users/arigatatsuya/Work/git/github.com/sk8metalme/ai-agent-setup`

**日時**: 2026-01-31T14:01:46.410Z

---

以下のタスクを実行してください：

1. `/tmp/knowledge_candidates_2026-01-24.json` から、便利なコマンドやツールに関連する知識を抽出
   - "command", "script", "tool", "cli", "git", "npm", "docker", "bash", "コマンド", "ツール" などを含む候補
   - 実行可能なコマンド例があるものを優先
   - 単なる実行ログは除外

2. 抽出した知識を以下の形式でJSON配列として保存（`/tmp/knowledge_ops_2026-01-24.json`）:
   ```json
   [
     {
       "title": "知識のタイトル（具体的で検索可能なもの）",
       "content": "知識の本文（マークダウン形式、コマンド例を含む）",
       "tags": ["command", "具体的なタグ1", "具体的なタグ2"],
       "category": "ops",
       "source_file": "元のソースファイル",
       "confidence": 0.8
     }
   ]
   ```

3. 最大30件まで抽出（高品質なもののみ）

重要：
- 質問やユーザーへの確認は一切行わない
- 自己判断で価値があると思われるものを抽出
- タイトルは日本語で、内容を端的に表現する
- confidenceスコアは、知識の価値を0-1で評価

出力：
- 抽出した知識の件数
- 主なトピック（3-5個）
- 保存したファイルパス
