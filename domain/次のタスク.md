# 次のタスク

**プロジェクト**: `/Users/arigatatsuya/Work/git/github.com/sk8metalme/ai-agent-setup`

**日時**: 2026-01-31T12:22:46.044Z

---

現在 `main` ブランチにいます。チーム開発標準に従って、次のタスクは以下の順序で進めるべきです：

## 次のタスク

1. **feature ブランチ作成**
   - main ブランチでの直接作業は禁止されているため、feature ブランチを作成します
   - 推奨ブランチ名: `feature/notify-ghostty-support` または `feature/notify-config-file`

2. **変更をコミット**
   - `global/hooks/notify.sh`（修正）
   - `global/hooks/notify.conf`（新規）

3. **グローバル設定インストール**
   - `./install-global.sh` を実行して `~/.claude/hooks/` に配布

4. **動作確認**
   - 設定ファイルあり/なしでの動作確認
   - Ghostty ユーザーの場合は Ghostty での動作確認

5. **PR作成（オプション）**
   - 必要に応じて GitHub PR を作成

どのように進めますか？以下から選択してください：

1. **自動で進める** - feature ブランチ作成 → コミット → インストール → 動作確認まで一括実行
2. **手動で進める** - 各ステップごとに確認しながら進める
3. **ブランチ名を指定** - 別のブランチ名で進める
